# Dépendances
/node_modules

# Build (régénéré via `npm run build`)
/dist

# Paquets de release (régénérés via `npm run package`)
/release

# Clés de signature et paquets Chrome (NE JAMAIS COMMITTER une clé privée)
*.pem
*.crx

# Résidus Chrome (extension chargée non empaquetée)
**/_metadata

# Logs / divers
*.log
npm-debug.log*
.DS_Store
Thumbs.db
.env
.vscode
.idea
